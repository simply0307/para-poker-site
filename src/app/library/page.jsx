import { FuturePage } from "@/components/eggs/FuturePage";

export const metadata = { title: "Library" };
export default function LibraryPage() {
  return <FuturePage eyebrow="EGGS / Library" title="Keep what matters to you."><p>A personal library for saved content, publications, and artifacts is planned. Saving and collections will become available with EGGS accounts.</p></FuturePage>;
}

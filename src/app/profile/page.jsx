import { FuturePage } from "@/components/eggs/FuturePage";

export const metadata = { title: "Profiles" };
export default function ProfilesPage() {
  return <FuturePage eyebrow="EGGS / Profiles" title="Your handle. Your story."><p>EGGS profiles are planned as a shared home for your pseudonymous identity. Choose how you present yourself and which project connections you share.</p><p>Your league records will stay with each league. A profile will bring those connections together, with links to the full story.</p></FuturePage>;
}
